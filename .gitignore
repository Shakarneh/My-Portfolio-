# ── Local media working area — never committed ───────────────
# Camera originals and uncompressed clips. The web-ready copies
# live in assets/ ; everything else stays on my machine.
photos and videos/
media-library/

# ── OS junk ──────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store

# ── Editors ──────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Python (asset tooling) ───────────────────────────────────
__pycache__/
*.py[cod]
.venv/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
